spring:
  datasource:
    url: jdbc:mysql://localhost:3306/drama?useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Shanghai&connectionCollation=utf8mb4_unicode_ci
    username: root
    password: ${SPRING_DATASOURCE_PASSWORD:}

cos:
  secret-id: ${COS_SECRET_ID:}
  secret-key: ${COS_SECRET_KEY:}
  playback-proxy-base-url: ${COS_PLAYBACK_PROXY_BASE_URL:}

rag:
  base-url: ${RAG_BASE_URL:http://localhost:8091}
  callback-base-url: ${BACKEND_CALLBACK_BASE_URL:http://localhost:8080/api/v1}
