# Default dependencies for local development and CPU/MPS runtime.
# Recommended Python: 3.10 or 3.11.
#
# This file intentionally does not install NVIDIA CUDA or PaddleOCR backend
# packages. Use requirements-linux-cuda.txt on Linux x86_64 GPU machines, or
# requirements-ocr-cpu.txt when you need PaddleOCR on a supported CPU platform.

# LLM API / env
openai>=1.40.0,<2.0.0
python-dotenv>=1.0.1,<2.0.0

# Video / image processing
opencv-python-headless>=4.9.0.80,<5.0.0
numpy>=1.26.0,<3.0.0
tqdm>=4.66.0,<5.0.0

# RAG semantic recall / BGE rerank
torch>=2.6.0,<3.0.0
sentence-transformers>=3.0.0,<6.0.0
FlagEmbedding>=1.3.0,<2.0.0

# ASR: faster-whisper uses CTranslate2. CPU works everywhere supported;
# Linux CUDA extras live in requirements-linux-cuda.txt.
faster-whisper>=1.1.0,<2.0.0
ctranslate2>=4.7.0,<5.0.0

# Optional but useful for model downloads / audio-video handling
huggingface-hub>=0.24.0
ffmpeg-python>=0.2.0
